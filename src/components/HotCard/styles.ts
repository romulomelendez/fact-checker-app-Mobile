import styled from 'styled-components/native'

export const HNContainer = styled.View `

    flex-direction: column;
    gap: 10px;
    /* border: 1px solid #cecece; */
    border-radius: 8px;
    padding: 10px;
    margin: 5px;
    width: 400px;
    background-color: #cecece;

`

export const HNHeader = styled.View `

    flex-direction: column;
    justify-content: center;
    align-items: baseline;

`

export const HNTitle = styled.Text ``

export const HNSubtitle = styled.Text ``

export const HNBody = styled.View `

    width: 100%;
    justify-content: space-between;

`

export const HNButton = styled.TouchableOpacity `

    border: none;
    outline: none;
    text-decoration: none;

`

export const HNPublishDate = styled.Text ``